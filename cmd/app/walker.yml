services:
  - name: ExampleTask
    http:
      - route: /user
        schema: http
        method: put
        port: 3333
        header:
          Content-Type: application/json
        json_body:
          name: test
          password: $flag